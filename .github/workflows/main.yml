name: Demo
on: [push]

jobs:
  Demo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🐧"
      - uses: ./.github/actions/install-conan
#      - run: "${CONAN}" --version
#        env:
#          CONAN: ${{steps.0.outputs.conan}} 

