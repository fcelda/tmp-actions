name: Demo
on: [push]

jobs:
  Demo:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/actions/install-conan
        id: install-conan
      - run: "${CONAN}" --version
        env:
          CONAN: ${{steps.0.outputs.conan}} 
      - run: echo "🐧"
