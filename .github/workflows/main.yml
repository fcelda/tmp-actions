name: Demo
on: [push]

jobs:
  Demo:
    runs-on: ubuntu-latest
    steps:
      - uses: .github/actions/install-conan
        id: install-conan
      - env:
          CONAN=${{steps.install-conan.outputs.conan}} 
        run: "${CONAN}" --version
      - run: echo "🐧"
